package com.healthvault.ui;

/**
 * Legacy stub — UI logic has moved to {@link LoginView} (pure Java, no FXML).
 * @deprecated Use LoginView
 */
@Deprecated
public class LoginController {
    // intentionally empty – kept for backward compatibility only
}
