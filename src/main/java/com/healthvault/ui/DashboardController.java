package com.healthvault.ui;

/**
 * Legacy stub — UI logic has moved to {@link DashboardView} (pure Java, no FXML).
 * @deprecated Use DashboardView
 */
@Deprecated
public class DashboardController {
    // intentionally empty – kept for backward compatibility only
}
